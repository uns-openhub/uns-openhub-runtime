#!/bin/sh
set -eu

case "$0" in
  */*) bin_dir=${0%/*} ;;
  *) bin_dir=. ;;
esac
runtime_dir=$(CDPATH= cd -- "$bin_dir/.." && pwd -P)
exe=$("$bin_dir/runtime-download" uns)
UNS_RUNTIME_ROOT="$runtime_dir" exec "$exe" "$@"
